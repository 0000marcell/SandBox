oauth2-password-grant - 'app/authenticators/oauth2.js'
	authenticator: fetch the token at /token
authenticated-route-mixin = inject on the routes
	verify if the token is available, if not a 401 error is thrown 
application-route-mixin - inject on the application route
	when a error is thrown by the authenticated-route-mixin, this mixin will 
	transition to the login page
dataadaptermixin - inject on the adapter 
	send a authorization header
oauth2-bearer - app/authorizer/application.js
	creates authorization data that can be inject in to outgoing msg

Custom service 
session - extend ESASession 
	put a observer on isAuthenticated and grab the current user
