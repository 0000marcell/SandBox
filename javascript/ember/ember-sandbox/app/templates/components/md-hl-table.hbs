{{#md-table content=content as |row|}}
	{{#each columnIds as |id|}}
		{{#if id.link}}
			{{#md-table-col row=row}}
				{{link-to (dynValue row id.key) id.link row}}
			{{/md-table-col}}
		{{else}}
			{{md-table-col row=row valueBindingPath=id.key}}
		{{/if}}
	{{/each}}
{{/md-table}}
