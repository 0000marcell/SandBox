import { dynValue } from 'ember-sandbox/helpers/dyn-value';
import { module, test } from 'qunit';

module('Unit | Helper | dyn value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dynValue([42]);
  assert.ok(result);
});
