import { escapeCSS } from 'app4/helpers/escape-css';
import { module, test } from 'qunit';

module('Unit | Helper | escape css');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = escapeCSS([42]);
  assert.ok(result);
});
